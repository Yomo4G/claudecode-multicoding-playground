# Scope Documentation Rule

## 基本方針

- すべてのタスクはスコープ（やること）と非スコープ（やらないこと）を文書化しなければならない
- スコープが未定義のタスクは実行を開始してはならない

## スコープの決定

- AIがスコープと非スコープを提案する
- 人間が承認してからタスクが確定する

## スコープ外の作業が発生した場合

- 新しいタスクとして切り出す
- 現タスクの実行はそのまま続行する
- 切り出した新タスクにもスコープと非スコープを定義する
